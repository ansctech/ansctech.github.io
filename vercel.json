{
  "version": 2,
  "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/" }],
  "projectSettings": {
    "framework": "Node.js",
    "buildCommand": "npm run build",
    "outputDirectory": "./"
  }
}
